import 'package:flutter/material.dart';

//border radius for all the containers
// in order to make all the containers, buttons have the same borderRadius
BorderRadius roundedRadius = BorderRadius.circular(10);
